# Backlog

## Follow-ups

- Add browser-matrix execution against installed Chrome, Edge, and Firefox/ScriptCat builds in CI-capable environments.
- Add an opt-in Chrome DevTools Protocol screenshot backend for pixel-exact captures when a browser is launched with an explicitly authorized debugging port. The current capture is DOM-rendered and reports that method.
- Add POSIX CI runners so Windows/MSYS/WSL path unit coverage is complemented by native Linux and macOS process-lifecycle coverage.
- Add cryptographic installation package signatures if VibeCat is distributed outside local source-controlled repositories.
